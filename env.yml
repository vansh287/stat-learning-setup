name: stats-learning-env
channels:
  - conda-forge  # Must come first for PyMC
  - defaults
dependencies:
  - python=3.11
  - pymc=5.0  # Explicit version from conda-forge
  - numpyro
  - scikit-learn
  - pandas
  - matplotlib
  - jupyter
  - pip:
    - pymc-ext>=0.1.4  # Sometimes needed for PyMC v5
